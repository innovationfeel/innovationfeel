= render 'home/header', home: false, admin: true

#new-post.container
  .row
    .col-md-8.col-md-offset-2
      h2.text-center = t('a.add_post')
      = form_for(@post, as: :post, url: admin_posts_path) do |field|
        = render 'form', field: field
